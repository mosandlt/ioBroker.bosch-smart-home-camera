# macOS
.DS_Store

# Editors
.vscode/*
!.vscode/settings.json
.idea/
*.swp

# Node.js (future)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
*.tgz
yarn.lock

# Build / dist (committed for ioBroker adapter — do not re-add build/ here)
# build/ is intentionally committed (E124: repochecker needs build/main.js on GitHub)
*.tsbuildinfo

# Adapter-local config (real credentials never committed)
iobroker-data/
.env
.env.local

# Coverage
coverage/
*.lcov
.nyc_output/

# Local working drafts (community outreach prep, not project content)
issue-*-comment*.md
forum-*-reply*.md
update-posts-*.md
simon42-*.md

# release-script intermediate metadata
.commitinfo

# Integration tests rely on live cameras + manual env vars — keep out of CI/release
test/integration/
backups/

# vis-2 widget build
src-widgets/node_modules/
src-widgets/build/
